query CharacterById($id: String!) {
    characterById(id: $id) {
        id
        user {
            id
            email
            displayName
            userImage
        }
        name
        avatarImage
        speed
        initiativePoint
        hitDice
    }
}
